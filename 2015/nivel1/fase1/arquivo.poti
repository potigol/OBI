# Arquivos
ent = leia_inteiros(" ") 
n, b = ent[1], ent[2]
num = leia_inteiros(" ").ordene

var x, y := 1, n
enquanto x<=y faça
  se num[x] + num[y] <= b então
    x := x + 1
  fim
  y := y - 1
fim
escreva n - y
